# Docker Role

Installation and configuration of Docker and Docker Compose for containerized applications

## Purpose
Installs Docker CE and Docker Compose plugin, configures user access for container management without sudo
